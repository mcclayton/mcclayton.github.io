import { style } from '@vanilla-extract/css';

export const sourceCodeContainer = style({
  display: 'inline-block',
});

export const emojiImage = style({
  width: 14,
  height: 14,
  marginLeft: '0.5rem',
});
